<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/iv_euro_standing_banner"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_euro_standing"
            app:layout_constraintDimensionRatio="W,1:6"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <Button
            android:id="@+id/btn_euro_standings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/_10sdp"
            android:fontFamily="@font/poppins"
            android:text="See Standings"
            app:layout_constraintBottom_toBottomOf="@id/iv_euro_standing_banner"
            app:layout_constraintEnd_toEndOf="@id/iv_euro_standing_banner"
            app:layout_constraintTop_toTopOf="@+id/iv_euro_standing_banner" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>