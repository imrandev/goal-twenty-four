<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_offline_status">

        <ImageView
            android:id="@+id/iv_review_banner"
            app:layout_constraintTop_toTopOf="parent"
            android:src="@drawable/ic_review_banner"
            android:layout_width="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintDimensionRatio="W,1:6"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            android:layout_height="0dp"/>

        <Button
            android:id="@+id/btn_review"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/_30sdp"
            android:layout_marginEnd="@dimen/_10sdp"
            android:text="Make Review"
            android:textColor="@android:color/white"
            android:textSize="@dimen/_8sdp"
            android:fontFamily="@font/poppins"
            android:background="@drawable/review_button_background"
            app:layout_constraintTop_toTopOf="@+id/iv_review_banner"
            app:layout_constraintEnd_toEndOf="@id/iv_review_banner"
            app:layout_constraintBottom_toBottomOf="@id/iv_review_banner"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>